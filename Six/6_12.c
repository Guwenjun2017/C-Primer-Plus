#include<stdio.h>
int main(void)
{
    float a=1.0;
    float b,result;
    int i,limit;

    while((scanf("%d",&limit)) > 0)
    {
	if(limit > 0)
	{
	    result=
	    printf("result:%f",result);
	}
    }
}
